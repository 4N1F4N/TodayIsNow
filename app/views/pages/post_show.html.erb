<% if session[:user_id]%>
    <%= render partial: "posts/post_pattern", locals: { id: params[:id], user: User.find_by(id:session[:user_id]) }%>
<% else %>
    <%= render partial: "posts/post_pattern", locals: { id: params[:id] }%>
<% end %>