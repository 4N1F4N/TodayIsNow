<!DOCTYPE html>
<html>
  <head>
    <title>TodayIsNow</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body style = 'text-align: center'>
    <div class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <div class="nav-brand">
          <a class="btn btn-primary me-2" href=<%= main_path %>>Главная</a>
        </div>
        <% if session[:user_id] && User.find_by(id:session[:user_id]).can_see_closes_posts? %>
          <div class="collapse navbar-collapse">
            <a class="btn btn-primary me-2" href= <%="/post/closes"%>>Закрытые посты</a>
          </div>
        <% end %>
        <div class="d-flex">
          <% if session[:user_id] %>
            <a class="btn btn-primary me-2" href= <%="/user/#{session[:user_id]}"%>>Вы уже зашли</a>
            <% if User.find_by(id:session[:user_id]).can_create_post? %>
              <a class="btn btn-primary me-2" href= <%="/post/create"%>>Создать пост</a>
            <% end %>
          <% end %>
          <a class="btn btn-primary me-2" href=<%= user_create_path %>>Создать</a>
          <a class="btn btn-primary me-2" href=<%= user_login_path %>>Войти</a>
        </div>
      </div>
    </div>
    <div style = 'padding: 0 40%;'>
      <%= yield %>
    </div>
  </body>
</html>
